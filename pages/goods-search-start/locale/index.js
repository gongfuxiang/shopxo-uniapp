import { createPageLocaleMixin } from '@/locale/merge-page-locale.js';
var zh = {
    pages: {
        "goods-search-start": "数据搜索"
    }
};
var en = {
    pages: {
        "goods-search-start": "Data search"
    }
};
export default createPageLocaleMixin({ zh, en });
